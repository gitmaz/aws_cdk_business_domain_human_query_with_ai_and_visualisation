# Base URL from CDK output HttpApiUrl (no trailing slash).
# After LocalStack deploy: npm run playwright:print-env
PLAYWRIGHT_API_BASE_URL=https://xxxxxxxx.execute-api.us-east-1.amazonaws.com

# Optional: override CloudFormation stack name (default BusinessDomainHumanQuery-local)
# PLAYWRIGHT_CFN_STACK=BusinessDomainHumanQuery-local
